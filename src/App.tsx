import Auth from './components/Auth'; // 作成したAuthコンポーネントをインポート
import './App.css'; // App.cssをインポート（今は使いませんが残しておきます）

function App() {
  return (
    <div>
      <Auth />
    </div>
  );
}

export default App;